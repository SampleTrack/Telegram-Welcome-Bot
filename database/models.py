# Default group document structure in MongoDB

DEFAULT_GROUP = {
    "chat_id": None,
    "welcome_message": None,
    "rules": None,
    "welcome_enabled": True,
    "rules_button": True,
    "clean_joins": True,
    "ban_bots": True,
}
